<?xml version="1.0" encoding="utf-8"?>
<Project Sdk="Microsoft.NET.Sdk">
	<PropertyGroup>
		<TargetFrameworks>net40;net45;netstandard2.0</TargetFrameworks>

		<RuntimeIdentifiers>win7-x86;win7-x64</RuntimeIdentifiers>
		<Authors>ArtZilla</Authors>
		<RequireLicenseAcceptance>false</RequireLicenseAcceptance>
		<ProjectGuid>{F0272B4C-10AE-4BD1-8B65-09ABEABFAA90}</ProjectGuid>
		<OutputType>Library</OutputType>

		<LangVersion>latest</LangVersion>
		<WarningLevel>4</WarningLevel>
		<AppDesignerFolder>Properties</AppDesignerFolder>

		<Language>en-US</Language>
		<NeutralLanguage>en-US</NeutralLanguage>

		<PackageId>ArtZilla.Config</PackageId>
		<AssemblyName>ArtZilla.Config</AssemblyName>
		<RootNamespace>ArtZilla.Config</RootNamespace>

		<AssemblyTitle>Configuration Framework</AssemblyTitle>
		<Description>Configuration Framework</Description>
		<Summary>Configuration Framework</Summary>

		<PackageCopyright>Copyright (c) 2017 Artyom Fomin</PackageCopyright>
		<PackageTags>ArtZilla, .Net, Configuration Framework</PackageTags>
		<PackageReleaseNotes>First version</PackageReleaseNotes>
		<PackageIconUrl>https://files.artzilla.name/ArtZilla.Config/Icon.png</PackageIconUrl>
		<PackageProjectUrl>https://files.artzilla.name/ArtZilla.Config</PackageProjectUrl>
		<PackageLicenseUrl>https://files.artzilla.name/ArtZilla.Config/License.txt</PackageLicenseUrl>

		<Copyright>Copyright (c) 2017 Artyom Fomin</Copyright>
		<RepositoryType>Github</RepositoryType>
		<Company>ArtZilla's development</Company>
		<Version>0.1.0.3</Version>
		<FileVersion>0.1.0.3</FileVersion>
		<AssemblyVersion>0.1.0.3</AssemblyVersion>
		<RepositoryUrl>https://github.com/RuBeArtZilla/ArtZilla.Config</RepositoryUrl>
	</PropertyGroup>

	<PropertyGroup Condition="'$(Configuration)'=='Debug'">
		<DefineConstants>DEBUG</DefineConstants>
		<IncludeSymbols>true</IncludeSymbols>
	</PropertyGroup>

	<PropertyGroup Condition=" '$(Configuration)' == 'Release' ">
		<GenerateDocumentationFile>true</GenerateDocumentationFile>
		<GeneratePackageOnBuild>True</GeneratePackageOnBuild>
		<DefineConstants>RELEASE</DefineConstants>
	</PropertyGroup>

	<PropertyGroup Condition=" '$(TargetFramework)' == 'net40'">
		<DefineConstants>NET40;NETFULL</DefineConstants>
	</PropertyGroup>
	<PropertyGroup Condition=" '$(TargetFramework)' == 'net45'">
		<DefineConstants>NET45;NETFULL</DefineConstants>
	</PropertyGroup>
	<PropertyGroup Condition=" '$(TargetFramework)' == 'netstandard2.0'">
		<DefineConstants>NETSTANDARD;NETSTANDARD2_0</DefineConstants>
	</PropertyGroup>

	<!-- Common NuGet Packages -->
	<ItemGroup>
		<PackageReference Include="ArtZilla.Net.Core" Version="1.1.0.8" />
		<PackageReference Include="System.ValueTuple" Version="4.4.0" />
	</ItemGroup>

	<!-- NET Framework 4.0 NuGet Packages -->
	<ItemGroup Condition=" '$(TargetFramework)' == 'net40'">
		
	</ItemGroup>
	
	<!-- NET Framework 4.5 NuGet Packages -->
	<ItemGroup Condition=" '$(TargetFramework)' == 'net45'">
		<PackageReference Include="System.Reflection.Emit" Version="4.3.0" />			
	</ItemGroup>
	
	<!-- NET Standard 2.0 NuGet Packages -->
	<ItemGroup Condition=" '$(TargetFramework)' == 'netstandard2.0'">
		<PackageReference Include="System.Reflection.Emit" Version="4.3.0" />
	</ItemGroup>
</Project>