namespace ArtZilla.Net.Config.Tests.Generators;

[TestClass]
public sealed class GeneratorsTests : Core {                 
/*	[TestMethod]
	public void SimpleCopyCheck() {
		var cfr = ConfigManager.GetDefaultConfigurator();
		var src = cfr.Copy<ITestConfiguration>();
		var dst = new TestConfigurationCopy();
		ChangeConfig(src);
		if (AssertCfg.AreEqualAllProperties(dst, src))
			Assert.Fail();
		
		dst.Copy(src);
		AssertCfg.AssertEqualProperties(src, dst);
	}       

	[TestMethod]
	public async Task CollectionTest() {
		var config = ConfigManager.Realtime<IConfigListConfiguration>();
		Assert.IsInstanceOfType(config, typeof(ConfigListConfigurationNotify));
	}*/
}